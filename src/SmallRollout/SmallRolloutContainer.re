/*let component = ReasonReact.reducerComponent("SmallRolloutContainer");


type icon = 
  | Close(string)
  | Menu(string);

type menu = {
  active: bool,
  icon: icon
};

type state = {
  menu: menu
};

type action = 
  | MenuClicked;

let make = (_children) => {
  ...component,
  

};*/